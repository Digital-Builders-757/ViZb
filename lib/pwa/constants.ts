export const PWA_DISMISS_STORAGE_KEY = "vizb_pwa_install_dismissed_at"

/** 10 days — within the 7–14 day cooldown range from issue spec. */
export const PWA_DISMISS_COOLDOWN_MS = 10 * 24 * 60 * 60 * 1000

/** Delay before showing the banner so it does not flash on navigation. */
export const PWA_PROMPT_DELAY_MS = 4000
